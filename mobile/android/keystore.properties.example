# Copy to keystore.properties (same folder) after running: npm run keystore
# storeFile is relative to android/app/
storeFile=../keystore/emb-crm-release.jks
storePassword=CHANGE_ME
keyAlias=emb-crm
keyPassword=CHANGE_ME
