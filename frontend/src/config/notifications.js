/**
 * Real-time notification toasts, bell badge, and socket updates.
 * To turn back on: set VITE_NOTIFICATIONS_ENABLED=true in frontend/.env
 */
export const NOTIFICATIONS_ENABLED = import.meta.env.VITE_NOTIFICATIONS_ENABLED === 'true';
